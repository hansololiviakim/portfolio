export type HomeLinkType = {
  id: number
  title: string
  url: string
}

export type CommonTitleType = {
  title: string
  desc: string
}

export type ExperiencesType = {
  id: string
  title: string
  desc: string
  detail: string[]
  period: string
}
